The frontend application can be found in the folder src/main/resources/webpage.
It has to be deployed from root folder launching ng serve from the terminal.
The frontend calls to the api are configured based on localhost:8080, in case the port changes this routes need to be changed.

The backend app has to be deployed as a maven application.